
{
  "entities": {
    "MenuItem": {
      "title": "MenuItem",
      "description": "A menu item offered by the cafe.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "price": { "type": "number" },
        "category": { "type": "string", "enum": ["HOT", "COLD", "DESSERT"] },
        "emoji": { "type": "string" },
        "image": { "type": "string", "description": "Base64 encoded image data" }
      },
      "required": ["name", "price", "category"]
    },
    "Order": {
      "title": "Order",
      "description": "A customer order.",
      "type": "object",
      "properties": {
        "tableNumber": { "type": "string" },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "name": { "type": "string" },
              "price": { "type": "number" },
              "quantity": { "type": "number" }
            }
          }
        },
        "totalPrice": { "type": "number" },
        "timestamp": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["NEW", "PREPARING", "DELIVERED"] }
      },
      "required": ["tableNumber", "items", "totalPrice", "status"]
    },
    "Feedback": {
      "title": "Feedback",
      "description": "Customer feedback and ratings.",
      "type": "object",
      "properties": {
        "rating": { "type": "number" },
        "comment": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["rating", "timestamp"]
    },
    "Member": {
      "title": "Member",
      "description": "A loyalty club member.",
      "type": "object",
      "properties": {
        "phoneNumber": { "type": "string" },
        "points": { "type": "number" },
        "name": { "type": "string" },
        "level": { "type": "string" }
      },
      "required": ["phoneNumber", "points"]
    },
    "GalleryImage": {
      "title": "GalleryImage",
      "description": "An image for the cafe gallery.",
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "Base64 encoded image data" },
        "hint": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["url"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/menu/{itemId}": {
      "schema": "MenuItem",
      "description": "Collection of cafe menu items."
    },
    "/orders/{orderId}": {
      "schema": "Order",
      "description": "Collection of customer orders."
    },
    "/feedback/{feedbackId}": {
      "schema": "Feedback",
      "description": "Collection of customer feedback entries."
    },
    "/members/{memberId}": {
      "schema": "Member",
      "description": "Collection of loyalty club members."
    },
    "/gallery/{imageId}": {
      "schema": "GalleryImage",
      "description": "Collection of gallery images."
    }
  }
}
